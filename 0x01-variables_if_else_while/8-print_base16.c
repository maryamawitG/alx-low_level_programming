#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char ch;

	int b;

	ch = 'a';
	while
		(b < 10)
		{
			putchar(b + '0');
			b++;
		}
	while
		(ch <= 'f')
		{
			putchar(ch);
			ch++;
		}
	putchar('\n');
	return (0);
}
