#include "main.h"
#include<stdio.h>
/**
 * _puts - Entry point
 *@str: string
 * Return: Always 0 (Success)
 */
void _puts(char *str)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
